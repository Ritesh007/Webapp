deployment:
  dockerhub:
    branch: feature
    commands:
      - ls -ltr
      - cd Webapplication
      - ls -ltr
      - cd Webapplication
      - ls -ltr
      - pip install flask
      - nohup python Webapplication/webapp.py
      - curl -X GET http://127.0.0.1:5000/ -H 'cache-control: no-cache'
      - pkill webapp.py
#to skip the test cases
test:
  override:
    - echo "done"