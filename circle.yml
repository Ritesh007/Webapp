deployment:
  dockerhub:
    branch: feature
    commands:
      - ls -ltr
      - cd Webapplication
      - ls -ltr
      - python webapp.py
#to skip the test cases
test:
  override:
    - echo "done"